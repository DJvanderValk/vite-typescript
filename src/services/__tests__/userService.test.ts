import { expect, describe, it } from 'vitest';

// import userService from '../userService';

describe('test', () => {
	it('1', () => {
		expect(1).toBe(1);
	});
	
	it('2', () => {
		expect(1).toBe(2);
	});
});
